# HydroSim Mobile Fork

Copy of `HydroSim-V2.0` for Android/tablet work. **Do not edit the original folder.**

- **Original (untouched):** `../HydroSim-V2.0/`
- **This fork:** mobile layout, touch input, perf guards only
- **Rule:** scope all layout/behavior changes with `@media (max-width: 860px)` and/or `matchMedia('(pointer: coarse)')` so desktop stays identical

## Run

Same as V2.0 — local server required (`start_windows.bat`).

## Task queue

See chat / project board. Each task is sized for a single Composer session (one concern, 1–2 files, no full-file rewrites).
